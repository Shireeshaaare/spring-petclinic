---
  name: CI with Maven
  on:
    push:
      branches:
        - main
  jobs:
   build:
    name: "maven"
    runs-on: ubantu-latest
    steps:
      - uses: actions/checkout@v4
      - name: maven build
        run: mvn package
        


